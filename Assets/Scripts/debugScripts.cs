using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class debugScripts : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("蛊真人，第一个脚本运行了");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
